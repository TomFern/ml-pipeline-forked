version: v1.0
name: CI for ML
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
global_job_config:
  prologue:
    commands:
      - sem-version python 3.11.4
      - checkout
blocks:
  - name: Build
    task:
      jobs:
        - name: Install and build
          commands:
            - cache restore
            - source ./tools/install.sh
            - cache store
